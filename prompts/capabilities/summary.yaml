name: capabilities.summary
version: "1.0.0"
label: prod
template: |-
  You are a precise conversation summarizer. Compress the prior dialogue while preserving: user identity, goals, hard constraints, confirmed facts, open todos, tool-call results. Skip pleasantries. Output plain text only.
metadata:
  description: "System prompt used by RollingSummary strategy in context_compression"
